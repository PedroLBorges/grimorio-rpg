@if($errors->any())<div class="rounded-lg border border-red-800/30 bg-red-100 p-4 text-red-900"><ul class="list-inside list-disc">@foreach($errors->all() as $error)<li>{{ $error }}</li>@endforeach</ul></div>@endif
@isset($characters)<div><label class="block font-serif font-bold">Personagem</label><select name="character_id" required class="mt-1 w-full rounded-lg border-amber-800/40 bg-amber-50/70 focus:border-purple-800 focus:ring-purple-800"><option value="">Selecione uma ficha</option>@foreach($characters as $character)<option value="{{ $character->id }}" @selected(old('character_id') == $character->id)>{{ $character->name }}{{ $character->isOwner(auth()->user()) ? ' — proprietário' : ' — editor' }}</option>@endforeach</select></div>@endisset
<div><label class="block font-serif font-bold">Título</label><input name="title" value="{{ old('title', $sessionNote->title ?? '') }}" required maxlength="255" class="mt-1 w-full rounded-lg border-amber-800/40 bg-amber-50/70 focus:border-purple-800 focus:ring-purple-800"></div>
<div><label class="block font-serif font-bold">Data da sessão</label><input type="date" name="session_date" value="{{ old('session_date', isset($sessionNote) ? $sessionNote->session_date?->format('Y-m-d') : '') }}" class="mt-1 w-full rounded-lg border-amber-800/40 bg-amber-50/70 focus:border-purple-800 focus:ring-purple-800"></div>
<div><label class="block font-serif font-bold">Anotação</label><textarea name="content" rows="12" required maxlength="50000" class="mt-1 w-full rounded-lg border-amber-800/40 bg-amber-50/70 focus:border-purple-800 focus:ring-purple-800">{{ old('content', $sessionNote->content ?? '') }}</textarea></div>
<div class="flex gap-2"><button class="rounded-lg border border-amber-900 bg-gradient-to-r from-amber-800 to-amber-950 px-5 py-2 font-serif font-bold text-yellow-200">Salvar anotação</button><a href="{{ route('session-notes.index') }}" class="rounded-lg border border-stone-600 bg-stone-300 px-5 py-2 font-serif font-bold text-stone-900">Cancelar</a></div>
